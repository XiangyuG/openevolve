# Current Program Information
- Fitness: {fitness_score}
- Feature coordinates: {feature_coords}
- Focus areas: {improvement_areas}

{artifacts}

{developer_feedback}

# Program Evolution History
{evolution_history}

# Current Program
```{language}
{current_program}
```

# Task
Propose improvements to the program that will improve its FITNESS SCORE. You may
propose two kinds of change:

A) Any traditional, schema-preserving source-level optimization -- e.g. eliminating
   dead code or redundant computation, batching/hoisting repeated kernel reads into
   one read or a local variable, simplifying control flow, algebraic simplification
   or strength reduction -- AS LONG AS it does not change any BPF map's type, key
   layout, or value layout. Every change, including these, still needs a Witness and
   a Formula (pre/post-transformation) in the format below; heimdall verifies these
   changes directly by proving full equivalence, no relaxation needed.

B) These two schema-changing optimization types, which heimdall can independently
   verify via a relaxed check once a developer approves the witness:

   (1) Map value width narrowing: shrink the VALUE struct of a BPF hash/array map
       (fewer bytes) when the real value range actually used by the program never
       needs the current width -- e.g. an 8-byte counter that provably never exceeds
       a 4-byte range, or a struct field/padding that can be dropped or packed
       tighter. You must justify the range/size claim in that change's Witness.

   (2) Map merging: if two or more separate maps are looked up with the SAME key in
       the SAME code path, merge them into a single map whose value struct is the
       union of the original maps' fields, replacing multiple
       bpf_map_lookup_elem()/bpf_map_update_elem() calls on that key with one. You
       must justify in the Witness that every original field stays readable and
       writable at the same points and that the key sets match.

Do not propose any other kind of schema change -- changing a map's TYPE (e.g. HASH
-> PERCPU_HASH), reducing a map's KEY size/layout, changing BPF_ANY/BPF_NOEXIST
update semantics, or any sampling/caching that skips work on some calls. Heimdall
has no witness format for these yet, so such a proposal cannot be independently
checked and will not be trusted, even if you believe it would help. Do not
restructure algorithms, add or remove probes/SEC sections, or rename/reformat
unrelated code.

If this program has no safe opportunity for (A) or (B), propose the smallest
possible change that still qualifies (or none at all) and say so in the
explanation -- do not invent an unrelated optimization.

The system maintains diversity across these dimensions: {feature_dimensions}
Different solutions with similar fitness but different features are valuable.

IMPORTANT: This is a PROPOSAL step only. Do NOT write a SEARCH/REPLACE diff and do
NOT include any program source code. A developer will review your proposed
change(s) below and approve or reject each one individually; only after that
will you be asked, in a follow-up request, to actually produce the diff
implementing the approved ones. Describe what you would change, you are not
changing it yet.

This restriction does NOT apply to the ```witness block below -- that is not
program source, it is the required transformation-witness DSL for the change,
and you MUST still include one for every change you propose.

Describe each proposed change in exactly this format. The ```witness block is
the REQUIRED core of every change: if you cannot write a valid transformation
witness for a change, do not propose that change.

## Transformation-witness DSL (the ```witness block)

Three blocks, in this fixed order. `assumption` and `binding` are optional and
may be omitted; `observation` is required with at least one statement. Every
statement ends with `;`. Grammar: c2rust_translation/witness_dsl/GRAMMAR.bnf.

    assumption {{
        original.ctx.<field> in [<lo>, <hi>];   // value-range precondition; either side; hex / negative allowed
    }}
    binding {{
        original.<map>[k] = optimized.<map>[k]; // entry-wise correspondence between the two maps; k is a free key variable
    }}
    observation {{
        original.return = optimized.return;                     // outputs that MUST still match, for every input
        original.<map>[:].<field> = optimized.<map>[:].<field>;
    }}

A <path> is a name then any run of `.field`, `[k]` (k a variable), or `[:]`
(every entry of a map). `original.return` is the program's return value. These
words are reserved and may NOT be used as names: assumption binding observation
in ignore flag of original optimized.

## What these blocks mean to the checker (read before writing any assumption/binding)

`assumption` and `binding` are NOT independently verified -- heimdall's solver
takes them as given facts and proves `observation` using them as premises. If
an assumption or binding is actually false for some real input, the checker
can still report the change equivalent, because the false premise silently
ruled out the counter-example that would have caught it. Only write one if
it's something the real code already guarantees (an existing range check, a
type's natural width, a verifier-enforced bound) -- never write one just
because it would make the proof go through. If you cannot point to why an
assumption is actually true, leave `assumption`/`binding` empty and only
propose the change if it verifies as a plain equivalence-preserving change
without them (see "Choosing the blocks" below).

`observation` is the opposite: it's the claim being proven, not a premise, so
listing more outputs only makes the check stricter -- when in doubt, include
an output rather than omit it.

The one exception: when the DSL narrows a map's value/key width or fuses two
maps, heimdall independently cross-checks the claimed byte widths against the
object files' real BTF metadata before honoring it. Everything else here,
including any `assumption`/`binding` you write directly in the DSL, is
trusted on your word alone -- so it is the one place in this whole proposal
where being wrong has a real cost: a badly justified assumption can turn a
genuinely broken change into one the developer approves believing it was
proven safe.

Choosing the blocks:
  - Narrowing a map VALUE or KEY (fewer bytes): put the range that makes it
    lossless in `assumption` (on the ctx field it derives from), relate the two
    maps entry-wise in `binding`, and name the map in `observation`.
  - A plain equivalence-preserving change (hoisting a lookup, dropping a dead
    store, tightening a loop bound, a pure refactor): leave `assumption` and
    `binding` empty; `observation` lists only the outputs the change can affect
    -- usually just `original.return = optimized.return;`.

## Format

I propose the following changes:
(1) <one-line summary of the change>
    ```witness
    <the transformation-witness DSL program for this change>
    ```
    Example: `<short fragment of the OLD code>` -> `<short fragment of the NEW code>` (in <function/section name>)
    Witness: <2-4 plain-English sentences: why the ```witness above holds for every input>
    Formula (pre-transformation): (assert true)
    Formula (post-transformation): (assert true)
(2) <one-line summary of the change>
    ```witness
    <...>
    ```
    Example: `<...>` -> `<...>`
    Witness: <...>
    Formula (pre-transformation): (assert true)
    Formula (post-transformation): (assert true)

Leave the two "Formula (...): (assert true)" lines exactly as shown UNLESS this
change narrows a width; then instead make each a self-contained SMT-LIB 2 snippet
over shared Int inputs -- pre declares a constant `pre_result` for the OLD value,
post re-declares the same inputs plus a `post_result` for the NEW value -- proven
equal for every input. Only Int/Bool sorts, no bitvector ops (approximate a mask
with `mod`), nothing after the final `)`.

Each "Example" line must quote a real, short fragment (a line or part of a line) of
the actual before/after code you are proposing, wrapped in single backticks -- not a
triple-backtick block -- so a reviewer can see exactly where the change would happen
without you having written the whole diff. This is REQUIRED, not optional: even if
you label it "Old:"/"New:" instead of "Example:", or the change is a one-line
constant/macro edit, you must still quote the actual old and new text in backticks.
Never leave this line (or its Old:/New: equivalent) with no code after the label.
